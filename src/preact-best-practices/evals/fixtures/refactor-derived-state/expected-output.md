# Expected Output

- derive `hasUnread` during render instead of mirroring it into state
- keep panel-opening logic in the user interaction path if that behavior should be interaction-driven
- preserve the component API and avoid introducing extra abstractions
