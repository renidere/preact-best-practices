# Expected Output

- avoid recommending or introducing signals by default
- preserve the compat-sensitive import model unless there is a concrete failure
- focus on real issues instead of mass import churn
